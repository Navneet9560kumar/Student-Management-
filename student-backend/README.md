Request → Router (api/) → CRUD (crud/) → Model (models/) → DB
                        ↓
                   ActivityLog create hoga har action pe
                        ↓
                   Cloudinary upload (documents/photos)